// object - general object type
let person: object = { name: "Nagendra", age: 25 };

// specific object type using type or interface
type User = {
  name: string;
  age: number;
};

let user: User = { name: "Alice", age: 30 };
