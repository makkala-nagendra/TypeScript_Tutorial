// Literal - specific value as type
// Restricts value to a specific string, number, or boolean.
let direction: "up" | "down" = "up";
console.log(direction)
direction = "down"; // ✅
console.log(direction)
